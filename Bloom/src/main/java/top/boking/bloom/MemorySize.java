package top.boking.bloom;

/**
 * 布隆过滤器中的内存大小可选项
 * 已废弃，
 * (this enum class can wroter in HashCodeLib，but not necessary)
 */
@Deprecated
public enum MemorySize {
    MB,MB64,MB128,MB256,MB512,GB,GB2,GB4,GB8,GB16,GB32,GB64,GB128,GB256,GB512,TB,TB2,TB4,TB8,TB16,TB32,TB64,TB128
}
